{% set name = "paquo" %}
{% set version = "0.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/paquo-{{ version }}.tar.gz
  sha256: 3500c73b8ed9f2252c53ce50c1455666d26c13bc73196f856b336cab325bd66f

build:
  noarch: python
  entry_points:
    - paquo = paquo.__main__:main
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools-scm
  run:
    - dynaconf >=3,!=3.1.0
    - jpype1 >=1.0.1
    - packaging
    - python >=3.7
    - shapely
    - typing-extensions >=4.0

test:
  imports:
    - paquo
  commands:
    - pip check
    - paquo config --help
  requires:
    - pip

about:
  home: https://github.com/bayer-science-for-a-better-life/paquo
  summary: library for interacting with QuPath
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ap--
